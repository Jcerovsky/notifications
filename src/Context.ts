interface T {
  title: string;
  text: string;
  createdAt: number;
  photoUrl: string;
}

// const sendNotification = async () => {};
//
// const deleteNotification = async (id: string): void => {};
//
// export { deleteNotification, sendNotification };
